services:
  nginx:
    image: henahax/voice-server-api:latest
    container_name: henahax-voice-server-api
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Berlin
    ports:
      - 8082:3000
    restart: unless-stopped
